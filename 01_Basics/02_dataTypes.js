'use strict'; // Treat all JS code as newer version

// alert(3 + 3);  //we're using node js not browser

console.log('Hitesh');

let name = 'Hitesh';
let age = 3;
let isLoggedIn = true;
let address = null;
let state; //undefined: VARIABLE is defined but VALUE isnt assigned

/*--------Primitive Data Types---------------- */
//number => 2 to power 53
//biginit
//string => "----"
//boolean => true/fal se
//null => standalone value(empty value) / datatype object
//undefined => VARIABLE is defined but VALUE isnt assigned / datatype undefined
//symbol => unique

//object

console.log(typeof undefined);
